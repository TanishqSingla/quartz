Set of data satisfying the schema. An instance is the actual data stored in the database.

## Properties
- An Instance changes often
- In the SQL model of relations a [[Relations (Tables)|relation]] contains multiset of rows.