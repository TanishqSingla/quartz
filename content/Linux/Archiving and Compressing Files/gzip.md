- The program `gzip` (GNU Zip) is one of the current UNIX compression programs.
- Files created by gzip ends with `.gz` 

**Compressing file**
```sh
gzip file
```

**Uncompressing Files**
```sh
gunzip file.gz
```

Unlike [[tar]], gzip cannot create [[Archives|archives]].
